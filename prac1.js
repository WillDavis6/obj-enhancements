const createInstructor = (firstName, lastName) => {
    firstName,
    lastName    
}

const instructor = (favoriteNumber, value) => {
    [favoriteNumber], value
    firstName, "Colt"
}

const instructors = {
    firstName: "colt",
    sayHi() {
        return "Hi";
    },
    sayBye() {
        return this.firstName + " says bye!";
    }
};

function createanimal(species, verb, noise) {
    return {
        species,
        [verb]() {
            return noise;
        }
    }
}